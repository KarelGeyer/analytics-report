import React from 'react'

import { Form, Heading } from './styles'

const Admin = ({location}) => {
  console.log(location);

  return (
    <>
      <Heading> Administrace účtů </Heading>

      <Form>    
      </Form>
    </>
  );
};

export default Admin;
